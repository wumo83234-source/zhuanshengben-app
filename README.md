# 河南专升本上岸助手

这是一个静态 HTML 版本的专升本备考 app 原型，入口文件是 `index.html`。

## 本地运行

直接双击 `index.html` 即可打开。

也可以在当前目录启动本地服务：

```powershell
python -m http.server 5173
```

然后访问：

```text
http://localhost:5173/
```

页面使用 Tailwind CSS CDN、Google Fonts 和 DiceBear 头像服务，完整视觉效果需要联网加载这些外部资源。
